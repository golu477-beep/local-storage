import React from 'react'

const Cart = ( {link,className}) => {
  return (
    <div>
          <img src={link} alt="" className={`${className}`}/>
          <h1>golunrtgrtkot</h1>
    </div>
  )
}

export default Cart
